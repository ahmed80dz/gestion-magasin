$("flash").update("<% flash.each do |name, msg| %><%= escape_javascript(content_tag :div, msg, :id => "flash_#{name}") %><% end %>");
  <% flash.clear %>
Effect.BlindUp("produit_<%= @produit.id %>");
$("flash").blinddown(300);

